/**
 * “Pick only what matches condition”
 * 👉 Meaning:

It creates a new array with only items that pass a condition.

🧠 Think of it like:

“Keep only the items that are true for my condition.”

👉 Use it when:

you want only items that match a condition
you are selecting a subset

❓ Do I want to REMOVE some items?

➡️ filter()
 */